tags:
  - Операции с шутками
summary: Сгенерировать шутку
description: Сгенерировать новую шутку.
produces:
  - application/json
parameters:
  - in: query
    name: apikey
    required: true
    description: Передайте ключ полученый после регистрации
    schema:
      type: integer
responses:
"200":
  description: successful operation