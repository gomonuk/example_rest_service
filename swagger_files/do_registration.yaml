tags:
  - Операции с пользователем
summary: Регистрация пользователя
description: Регистрация пользователя и получение ключа. Логин не уникален, в отличии от ключа.
produces:
  - application/json
parameters:
  - in: query
    name: login
    required: true
    schema:
      type: object
      properties:
        login:
          type: string
          description: Придумайте имя пользователя,
      required:
        - login
responses:
"200":
  description: successful operation