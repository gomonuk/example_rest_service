tags:
  - Операции с шутками
summary: Обновить шутку
description: Обновить текст вашей шутки.
produces:
  - application/json
parameters:
  - in: query
    name: apikey
    required: true
    description: Передайте ключ полученый после регистрации
    schema:
      type: integer
  - in: query
    name: joke_id
    required: true
    description: Передайте id шутки
    schema:
      type: integer
  - in: query
    name: joke_text
    required: true
    description: Передайте новый текст шутки
    schema:
      type: string
responses:
"200":
  description: successful operation